<div class="row">
  <div class="col-md-10 col-md-offset-1">
    <table class="table">
      <colgroup>
        <col />
        <col style="width: 50%" />
        <col />
        <col />
        <col />
        <col />
      </colgroup>
      <thead>
        <tr>
          <th>Platz</th>
          <th>Name</th>
          <th>Spiele</th>
          <th><%= highscore_type == 'highscore_words' ? raw('Wörter <span class="glyphicon glyphicon-arrow-down"></span>') : link_to('Wörter', highscore_words_path) %></th>
          <th><%= highscore_type == 'highscore_points' ? raw('Punkte <span class="glyphicon glyphicon-arrow-down"></span>') : link_to('Punkte', highscore_points_path) %></th>
          <th><%= highscore_type == 'highscore_elo' ? raw('Stufe <span class="glyphicon glyphicon-arrow-down"></span>') : link_to('Stufe', highscore_elo_path) %></th>
        </tr>
      </thead>
      <tbody>
        <%= render partial: highscore_type %>
      </tbody>
    </table>
  </div>
</div>
